services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: kkiniplan
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Run npm run db:setup}
      POSTGRES_DB: kkiniplan
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - kkiniplan_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kkiniplan -d kkiniplan"]
      interval: 3s
      timeout: 3s
      retries: 20

volumes:
  kkiniplan_pgdata:
